diff --git a/build_scripts/build_info_collector.py b/build_scripts/build_info_collector.py
index 0c8becf49..61f259387 100644
--- a/build_scripts/build_info_collector.py
+++ b/build_scripts/build_info_collector.py
@@ -202,6 +202,8 @@ class BuildInfoCollectorMixin:
                 py_scripts_dir = py_prefix / "Scripts"
             else:
                 py_scripts_dir = py_prefix / "bin"
+            if not Path(py_include_dir).exists():
+                py_include_dir = py_prefix / "include" / f"python{py_version}"
             self.py_scripts_dir = py_scripts_dir
         else:
             # We don't look for an interpreter when cross-compiling.
diff --git a/build_scripts/options.py b/build_scripts/options.py
index ba4039e99..3de5b36e7 100644
--- a/build_scripts/options.py
+++ b/build_scripts/options.py
@@ -486,6 +486,7 @@ class CommandMixin:
                                            and not self.qtpaths and self.qt_target_path)):
             use_cmake = True
 
+        print(f"qtpaths_abs_path = {qtpaths_abs_path}")
         QtInfo().setup(qtpaths_abs_path, self.cmake, qmake_abs_path,
                        self.has_qmake_option,
                        use_cmake=use_cmake,
@@ -498,8 +499,8 @@ class CommandMixin:
             except Exception as e:
                 if not self.qt_target_path:
                     log.error(
-                        "Could not find Qt. You can pass the --qt-target-path=<qt-dir> option "
-                        "as a hint where to find Qt.\n")
+                        f"Could not find Qt. You can pass the --qt-target-path=<qt-dir> option "
+                        f"as a hint where to find Qt. {e}\n")
                 else:
                     log.error("Could not find Qt via provided option "
                               f"--qt-target-path={qt_target_path}\n")
diff --git a/sources/pyside6/cmake/PySideHelpers.cmake b/sources/pyside6/cmake/PySideHelpers.cmake
index 9891eab3f..b210c41ab 100644
--- a/sources/pyside6/cmake/PySideHelpers.cmake
+++ b/sources/pyside6/cmake/PySideHelpers.cmake
@@ -132,7 +132,7 @@ macro(collect_optional_modules)
     endif()
     list(APPEND ALL_OPTIONAL_MODULES WebChannel WebEngineCore WebEngineWidgets
          WebEngineQuick WebSockets HttpServer)
-    find_package(Qt${QT_MAJOR_VERSION}WebEngineQuick)
+    # find_package(Qt${QT_MAJOR_VERSION}WebEngineQuick)
     # For Windows and Linux, QtWebView depends on QtWebEngine to render content.
     # On Android and Apple platforms, QtWebView uses the native webview backend and
     # does not require QtWebEngine.
diff --git a/sources/pyside6/cmake/PySideSetup.cmake b/sources/pyside6/cmake/PySideSetup.cmake
index 72d6a1ccf..af767d76c 100644
--- a/sources/pyside6/cmake/PySideSetup.cmake
+++ b/sources/pyside6/cmake/PySideSetup.cmake
@@ -215,7 +215,7 @@ endforeach()
 find_package(Qt6 COMPONENTS Qml)
 
 # Whether to add libpysideremoteobjects
-find_package(Qt6 COMPONENTS RemoteObjects)
+#find_package(Qt6 COMPONENTS RemoteObjects)
 
 string(REGEX MATCHALL "[0-9]+" qt_version_helper "${Qt${QT_MAJOR_VERSION}Core_VERSION}")
 
diff --git a/sources/pyside-tools/CMakeLists.txt b/sources/pyside-tools/CMakeLists.txt
index b8e9152..26746ce 100644
--- a/sources/pyside-tools/CMakeLists.txt
+++ b/sources/pyside-tools/CMakeLists.txt
@@ -81,9 +81,13 @@
     foreach(directory ${directories})
-        install(DIRECTORY "${directory}"
-                DESTINATION bin
-                FILE_PERMISSIONS
-                OWNER_EXECUTE OWNER_WRITE OWNER_READ
-                GROUP_EXECUTE GROUP_READ
-                WORLD_EXECUTE WORLD_READ
-                PATTERN "android_utilities.py" EXCLUDE) # excluding the symlink
+        if(EXISTS "${directory}")
+            install(DIRECTORY "${directory}"
+                    DESTINATION bin
+                    FILE_PERMISSIONS
+                    OWNER_EXECUTE OWNER_WRITE OWNER_READ
+                    GROUP_EXECUTE GROUP_READ
+                    WORLD_EXECUTE WORLD_READ
+                    PATTERN "android_utilities.py" EXCLUDE) # excluding the symlink
+        else()
+            message(WARNING "${directory} does not exist and will not be installed")
+        endif()
     endforeach()
diff --git a/sources/pyside6/cmake/Macros/PySideModules.cmake b/sources/pyside6/cmake/Macros/PySideModules.cmake
index c54b86994..0bdb35dcc 100644
--- a/sources/pyside6/cmake/Macros/PySideModules.cmake
+++ b/sources/pyside6/cmake/Macros/PySideModules.cmake
@@ -213,5 +213,7 @@ macro(create_pyside_module module_name)
     if (CMAKE_CROSSCOMPILING)
         list(APPEND shiboken_command "--platform=${CMAKE_SYSTEM_NAME}"
                                      "--arch=${CMAKE_SYSTEM_PROCESSOR}"
                                      "--compiler-path=${CMAKE_CXX_COMPILER}")
+    elseif(CMAKE_HOST_APPLE)
+        list(APPEND shiboken_command "--compiler-path=${CMAKE_CXX_COMPILER}")
     endif()
