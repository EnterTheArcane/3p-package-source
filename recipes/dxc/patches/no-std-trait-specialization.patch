--- a/include/llvm/ADT/StringRef.h
+++ b/include/llvm/ADT/StringRef.h
@@ -571,23 +571,10 @@
 }
 
 // HLSL Change Starts
-// StringRef provides an operator string; that trips up the std::pair noexcept specification,
-// which (a) enables the moves constructor (because conversion is allowed), but (b)
-// misclassifies the the construction as nothrow.
-namespace std {
-  template<>
-  struct is_nothrow_constructible <std::string, llvm::StringRef>
-    : std::false_type {
-  };
-  template<>
-  struct is_nothrow_constructible <std::string, llvm::StringRef &>
-    : std::false_type {
-  };
-  template<>
-  struct is_nothrow_constructible <std::string, const llvm::StringRef &>
-    : std::false_type {
-  };
-}
+// This once specialised std::is_nothrow_constructible for StringRef to correct a
+// std::pair noexcept deduction. Specialising that trait is not allowed, and compilers
+// now reject it outright, so the workaround is gone; current standard libraries
+// deduce it correctly without help.
 // HLSL Change Ends
 
 #endif
