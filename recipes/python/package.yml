version: 3.10.13
rev: 1
targets: [Python]
platforms: tools
# The framework sits at the package root: the engine's LYPython.cmake looks for
# Python.framework/Versions/Current/bin relative to the package, not under a subfolder.
payload: "."
